import { ComplaintResponseTotal } from './complaint-response-total';

describe('ComplaintResponseTotal', () => {
  it('should create an instance', () => {
    expect(new ComplaintResponseTotal()).toBeTruthy();
  });
});
